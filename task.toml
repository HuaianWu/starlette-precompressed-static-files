schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/starlette-precompressed-static-files"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "kh081e751027f0ab88431ccc69cccc62"
task_id = "starlette-precompressed-static-files"
display_title = "Serve negotiated precompressed static files"
display_description = "Optionally negotiate Brotli and gzip sidecars in StaticFiles with representation-correct caching, ranges, and HEAD responses."
original_title = "为 Starlette StaticFiles 增加预压缩静态资源协商"
category = "feature_request"
language = "python"
repository_url = "https://github.com/Kludex/starlette"
base_commit_hash = "554f368809e0d891a699667faf0cfbb20057eeb2"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary 554f368809e0d891a699667faf0cfbb20057eeb2 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:kh081e751027f0ab88431ccc69cccc62-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
